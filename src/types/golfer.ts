export type Golfer = {
  lastName: string,
  firstName: string,
  rank: number,
  previousRank: number,
  events: number,
  totalPoints: number,
  avgPoints: number,
  pointsLost: number,
  pointsGained: number,
  id: number,
}
